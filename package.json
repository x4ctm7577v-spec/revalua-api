{
  "name": "revalua-server",
  "version": "1.0.0",
  "description": "Servidor backend de Revalúa: conecta la app con la API de Claude sin exponer la clave.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  }
}
